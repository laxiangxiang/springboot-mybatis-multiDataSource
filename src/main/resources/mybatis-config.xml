<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="mybatis.properties">
        <!--其中的属性就可以在整个配置文件中被用来替换需要动态配置的属性值-->
        <!--如果资源文件中没有对应的属性，会使用下列对应属性的值-->
        <property name="datasource.mysql.username" value="root"/>
        <property name="datasource.mysql.password" value="12345"/>
    </properties>

    <settings>
        <setting name="cacheEnabled" value="true"/>
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="multipleResultSetsEnabled" value="true"/>
        <setting name="useColumnLabel" value="true"/>
        <setting name="useGeneratedKeys" value="false"/>
        <setting name="autoMappingBehavior" value="PARTIAL"/>
        <setting name="autoMappingUnknownColumnBehavior" value="WARNING"/>
        <setting name="defaultExecutorType" value="SIMPLE"/>
        <setting name="defaultStatementTimeout" value="25"/>
        <setting name="defaultFetchSize" value="100"/>
        <setting name="safeRowBoundsEnabled" value="false"/>
        <setting name="mapUnderscoreToCamelCase" value="false"/>
        <setting name="localCacheScope" value="SESSION"/>
        <setting name="jdbcTypeForNull" value="OTHER"/>
        <setting name="lazyLoadTriggerMethods" value="equals,clone,hashCode,toString"/>
    </settings>

    <environments default="development">
        <environment id="mysql">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${datasource.mysql.driver}"/>
                <property name="url" value="${datasource.mysql.url}"/>
                <property name="username" value="${datasource.mysql.username}"/>
                <property name="password" value="${datasource.mysql.password}"/>
            </dataSource>
        </environment>
        <environment id="oracle">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="${datasource.oracle.driver}"/>
                <property name="url" value="${datasource.oracle.url}"/>
                <property name="username" value="${datasource.oracle.username}"/>
                <property name="password" value="${datasource.oracle.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <!--<mapper class="com.example.demo.mapper.RawMaterialMapper"/>-->
        <mapper resource="mapper/RawMaterialMapper.xml"/>
        <mapper resource="mapper/CutPartMapper.xml"/>
    </mappers>
</configuration>